{
  "name": "ai-startup-validation-engine-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for the AI Startup Validation Engine",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test:unit": "node --test",
    "test": "npm run test:unit && node scripts/smoke-test.js && node scripts/security-test.js && node scripts/auth-test.js",
    "test:mongo": "node scripts/run-mongo-tests.js",
    "seed:demo": "node scripts/seed-demo.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^7.1.1",
    "morgan": "^1.10.0",
    "nodemailer": "^8.0.4",
    "stripe": "^21.0.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.0.1"
  }
}
